const { url, publishableKey } = window.KORBUILD_SUPABASE;
const db = window.supabase.createClient(url, publishableKey, { auth: { persistSession: true, autoRefreshToken: true, detectSessionInUrl: true } });
const $ = id => document.getElementById(id);
function showAuthError(message){$('side-company').textContent='KORbuild Demo';$('company-pill').textContent='KORbuild Demo';$('user-name').textContent='Owner';$('user-email').textContent=message||'Session unavailable';$('employees').textContent='—';$('teams').textContent='—';$('pending').textContent='—';$('period-value').textContent='—';$('period-label').textContent='Sign in required'}
function closeMenu(){$('user-menu')?.classList.add('hidden');$('user-menu-btn')?.setAttribute('aria-expanded','false')}
async function loadHome(){const{data:{session},error:sessionError}=await db.auth.getSession();if(sessionError||!session?.user){showAuthError('Please sign in to continue.');return}const user=session.user;const{data:profile,error:profileError}=await db.from('usuarios').select('id,name,empresa_id,active,empresas(name)').eq('id',user.id).maybeSingle();if(profileError||!profile){showAuthError('Profile setup required');return}const company=profile.empresas?.name||'KORbuild Demo';const name=profile.name||user.user_metadata?.full_name||'Owner';$('side-company').textContent=company;$('company-pill').textContent=company;$('user-name').textContent=name;$('user-email').textContent=user.email||'';$('user-avatar').textContent=name.charAt(0).toUpperCase();$('menu-avatar').textContent=name.charAt(0).toUpperCase();$('menu-full-name').textContent=name;$('menu-full-email').textContent=user.email||'';const[people,teams,period,evaluations,config,cycle]=await Promise.all([db.from('colaboradores').select('*',{count:'exact',head:true}).eq('empresa_id',profile.empresa_id).eq('active',true),db.from('equipes').select('*',{count:'exact',head:true}).eq('empresa_id',profile.empresa_id),db.from('periodos').select('id,start_date,end_date,status').eq('empresa_id',profile.empresa_id).order('start_date',{ascending:false}).limit(1).maybeSingle(),db.from('lancamentos').select('*',{count:'exact',head:true}).eq('empresa_id',profile.empresa_id).neq('status','COMPLETED'),db.from('configuracoes_operacionais').select('id',{count:'exact',head:true}).eq('empresa_id',profile.empresa_id),db.from('bonus_cycles').select('id',{count:'exact',head:true}).eq('empresa_id',profile.empresa_id)]);$('employees').textContent=people.count??0;$('teams').textContent=teams.count??0;$('pending').textContent=evaluations.count??0;const setupComplete=(config.count??0)>0&&(cycle.count??0)>0;if(setupComplete){$('setup-alert')?.classList.add('hidden');$('workspace-status-title').textContent='You\'re all set to begin.';}else{$('setup-alert')?.classList.remove('hidden');$('setup-count').textContent='1 of 7';$('workspace-status-title').textContent='Setup in progress.'}if(period.data){const start=new Date(period.data.start_date+'T00:00:00'),end=new Date(period.data.end_date+'T00:00:00');$('period-value').textContent=`${start.toLocaleDateString('en-US',{month:'short',day:'numeric'})} – ${end.toLocaleDateString('en-US',{month:'short',day:'numeric'})}`;$('period-label').textContent=period.data.status||'Prepared'}else{$('period-value').textContent='—';$('period-label').textContent='No period prepared'}}
$('user-menu-btn')?.addEventListener('click',e=>{e.stopPropagation();const menu=$('user-menu');const hidden=menu.classList.toggle('hidden');$('user-menu-btn').setAttribute('aria-expanded',String(!hidden))});document.addEventListener('click',e=>{if(!e.target.closest('.user-menu-wrap'))closeMenu()});$('menu-logout')?.addEventListener('click',async()=>{await db.auth.signOut();window.location.href='index.html'});db.auth.onAuthStateChange(event=>{if(event==='SIGNED_OUT')window.location.href='index.html'});$('start-setup')?.addEventListener('click',()=>window.location.href='setup.html');$('prepare')?.addEventListener('click',()=>window.location.href='setup.html');loadHome();